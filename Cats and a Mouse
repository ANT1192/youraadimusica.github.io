string catAndMouse(int x, int y, int z) {
    //check distance
    int xd= abs(x-z);
    int yd= abs(y-z);
    if(xd>yd)
    return "Cat B";
    else if(yd>xd)
    return "Cat A";
    else
     return "Mouse C";
}
